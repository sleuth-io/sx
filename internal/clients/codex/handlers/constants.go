package handlers

// Directory names for Codex configuration and assets
const (
	// ConfigDir is the global Codex config directory name
	ConfigDir = ".codex"
	// AgentsDir is the repo-scoped agents directory (Codex convention for repo skills)
	AgentsDir = ".agents"

	// Asset subdirectories
	DirSkills     = "skills"
	DirCommands   = "commands"
	DirMCPServers = "mcp-servers"
)
