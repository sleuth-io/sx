package handlers

// Configuration directory
const (
	// ConfigDir is the Cursor configuration directory name (in home dir)
	ConfigDir = ".cursor"
)

// Directory names for Cursor assets
const (
	DirSkills     = "skills"
	DirCommands   = "commands"
	DirMCPServers = "mcp-servers"
)
