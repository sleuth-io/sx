query AssetAuditLog($first: Int, $after: String) {
  assetAuditLog(first: $first, after: $after) {
    pageInfo {
      hasNextPage
      endCursor
    }
    nodes {
      id
      date
      actorEmail
      actorName
      event
      targetType
      targetName
      data
    }
  }
}
