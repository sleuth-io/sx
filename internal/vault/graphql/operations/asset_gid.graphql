query AssetGID($first: Int, $after: String) {
  vault {
    assets(first: $first, after: $after) {
      pageInfo {
        hasNextPage
        endCursor
      }
      nodes {
        id
        name
        slug
        type
      }
    }
  }
}
